# Disable PHP execution in the image upload directory
<FilesMatch "\.php[0-9]?$">
    Require all denied
</FilesMatch>

# Disable PHP engine as defense-in-depth
<IfModule mod_php.c>
    php_flag engine off
</IfModule>
<IfModule mod_php8.c>
    php_flag engine off
</IfModule>
